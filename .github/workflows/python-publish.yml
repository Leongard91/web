name: Testing
on: push

jobs:
  test_project:
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v2
    - name: Run Django unit tests
      run: |
        pip install --user django
        python Testing/lecture7/airline0/manage.py test
